import React from 'react';
import { JumpCircleLoading } from 'react-loadingg';

function Loading() {
    return (
        <div className="w-100 h-100 d-flex justify-content-center align-items-center">
            <JumpCircleLoading size="large" style={{}} />
        </div>
    );
}

export default Loading;